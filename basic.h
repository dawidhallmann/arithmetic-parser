#ifndef basic  
#define basic

int parse(std::string expresion){
    for( const auto &c : expresion ) {
    }
//    El* elements[MAX_LEN];
//
//    char lastChar;
//    char lastOperation;
//
//    for (int a=0; a < ex.length(); a++) {
//        char ch = ex[a];
//
//        if (ch >= '0' && ch <= '9'){
//            // it is number
//            if(lastChar){
//
//            }
//        }else if((ch == '*' || ch == '/') || (ch == '+' || ch == '-')){
//            // it is operation
//        }
//
//        lastChar = ch;
//
//        cout << ch << "\n";
//    }
    return 0;
}

#endif